

<div class="m-4 equipment-form d-flex justify-content-around">
  <%= form_tag equipments_path, method: :get do %>
    <div class="search-form-control form-group">
      <%= select_tag :sport, options_for_select(Equipment::SPORTS, params[:sport]),
        include_blank: true,
        class: "form-control"
      %>
      <%# <%= submit_tag "Search", class: "btn btn-primary" %>
      <button name="button" type="submit" class="btn btn-flat">
        <i class="fas fa-search"></i> Search
      </button>
    </div>
  <% end %>
</div>

<div class="row d-flex justify-content-between row-cols-md-4 g-4 m-5">
  <div style="height: 600px;" class="d-flex p-2 left-half"
    data-controller="mapbox"
    data-mapbox-markers-value="<%= @markers.to_json %>"
    data-mapbox-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>">
  </div>
  <% @equipments.each do |equipment| %>
      <div class="card">
        <% if equipment.photo.key? %>
          <%= cl_image_tag(equipment.photo.key, :class => "card-img-top p-3", :height => 400) %>
        <% end %>
        <div class="card-body">
          <h5 class="card-title"><%= equipment.title %></h5>
          <p class="card-text"> <i class="fas fa-map-marker-alt"></i> <%= equipment.location %></p>
        </div>
        <div class="card-footer d-flex justify-content-between">
            <%= link_to "Voir l'article", equipment, class: "btn btn-primary" %>
            <p><%= equipment.price %>€ par jour</p>
        </div>
      </div>
  <% end %>
</div>
