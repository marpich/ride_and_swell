<div class="container">

  <div class="box-welcome">
    <h4>Bienvenue dans la communauté 🌊</h4>
    <h5>Ride & Swell te propose de nombreux équipements de particuliers à louer au meilleur prix à côté de chez toi.</h5>
    <p>Inscris-toi et pratiques tous les sports nautiques que tu aimes très facilement!</p>
  </div>

  <div class="conection-form">
    <h4>Connexion</h4>
      <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <div class="form-inputs">
        <div class="d-grid col-3 mx-auto">
          <%= f.input :email,
                      required: false,
                      autofocus: true,
                      input_html: { autocomplete: "email" } %>
        </div>
        <div class="d-grid col-3 mx-auto">
          <%= f.input :password,
                      required: false,
                      input_html: { autocomplete: "current-password" } %>
        </div>
        <div class="d-grid col-3 mx-auto">
          <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
        </div>

        <div class="form-actions d-grid col-2 mx-auto">
          <%= f.button :submit, "Log in", class: "btn btn-primary m-3" %>
        </div>
      <% end %>
    <div class="d-grid col-3 mx-auto">
      <%= render "devise/shared/links" %>
    </div>

  </div>

</div>
